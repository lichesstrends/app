import { ReactNode } from 'react'

export function ResultSharesInfo(): ReactNode {
  return (
    <p className="mb-0 text-xs">
      Outcome shares across all games in the period. Hover bar for exact White/Draw/Black percentages.
    </p>
  )
}